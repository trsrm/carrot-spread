# Scripts

Small deterministic helpers for maintaining the personal brain.

These scripts must not delete files, rewrite raw source content, or expose sensitive content in generated summaries.

## Commands

```sh
python3 .scripts/bootstrap.py
python3 .scripts/validate_frontmatter.py
python3 .scripts/update_indexes.py
python3 .scripts/check_orphans.py
```
