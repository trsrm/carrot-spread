# Claude Code Entrypoint

Read `AGENTS.md` first. It is the canonical instruction layer.

Repo-local workflows live in `.agents/skills/`.

`.claude/skills/` contains only thin Claude Code pointers to those canonical skill files.
